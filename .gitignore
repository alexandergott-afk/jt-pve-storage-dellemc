*.deb
!releases/*.deb
*.changes
*.buildinfo
*.substvars
debian/.debhelper/
debian/jt-pve-storage-dellemc/
debian/debhelper-build-stamp
debian/files
debian/*.log
.claude/
ref/

# Internal documents - must not be published. The related projects keep the
# equivalents out of their repositories too.
jt-pve-storage-dellemc.md
CLAUDE.md

# Throwaway state directory the test suite writes to (see Makefile).
.test-state/
